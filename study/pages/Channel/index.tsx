import React from 'react';
import { Container, Header } from '@pages/Channel/styles';

const Channel = () => {
  return (
    <Container>
      <Header>채널입니다.</Header>
    </Container>
  );
};

export default Channel;